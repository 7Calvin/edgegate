## Summary

What does this PR change, and why?

## Related issues

Closes #

## Checklist

- [ ] `pytest` passes (backend) and `npm run build` passes (frontend).
- [ ] No secrets committed (`.env`, keys, certs, `.ovpn`, real domains/IPs).
- [ ] Shell scripts / systemd units stay LF (CRLF shebang breaks container exec).
- [ ] For changes touching a running appliance (install/update/PKI/DB), described how it was verified end-to-end.

## Notes for reviewers

Anything that needs special attention.
